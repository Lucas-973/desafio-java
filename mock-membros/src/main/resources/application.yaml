server:
  port: ${PORT:8090}

spring:
  application:
    name: mock-membros-api
  jackson:
    deserialization:
      fail-on-unknown-properties: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
